import { createSlice } from "@reduxjs/toolkit"

const initialState = {

}

export const { reducer: manageUserReducer, actions: manageUserActions } = createSlice({
    name: "manageBlood",
    initialState,
    reducers: {

    },
    extraReducers: (builder) => {
        
    }
})